
${for(content)}
@$it.resource_type$ {${if(it.rdmid)}${it.rdmid}${endif}
	${if(it.title)}title = "${it.title}",${endif}
	${if(it.creators)}authors = "${for(content.creators)}${it.name.family}, ${it.name.given}${sep} and ${endfor}",${endif}
	${if(it.pub_year)}year = "${it.pub_year}",${endif}
	${if(it.official_url)}url = "${it.official_url}",${else}url = "${it.id}",${endif}${endif}
	${if(it.issn)}issn = "${it.issn}",${endif}
	${if(it.isbn)}isbn = "${it.isbn}",${endif}
	${if(it.doi)}doi = "${it.doi}",${endif}
	${if(it.publisher)}publisher = "${it.publisher}",${endif}
	${if(it.publication)}publication = "${it.publication}",${endif}
	${if(it.volume)}volume = "${it.volume}",${endif}
	${if(it.number)}issue = "${it.number}",${endif}
	${if(it.collection)}collection = "${it.collection}",${endif}
	${if(it.pagerange)}pages = "${it.pagerange}",${endif}
	${if(it.pmcid)}pmcid = "${it.pmcid}",${endif}
}

${endfor}
